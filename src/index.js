import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './script'; // Import the Vanilla JavaScript code

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
